import styled from "styled-components/native";
import { TouchableOpacity } from "react-native";

export const BotaoComponent = styled(TouchableOpacity)`
    width:56px;
    height:56px;
    border-radius:4px;
    align-items:center;
    justify-content:center;
`;